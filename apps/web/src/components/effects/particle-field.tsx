/**
 * Subtle fixed atmospheric layer — pairs with `.magical-app-bg-guess` / `.magical-app-bg-codex` and `.particle-layer`.
 */
export function ParticleField() {
  return <div className="particle-layer" aria-hidden />;
}
